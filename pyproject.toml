[project]
name = "search-logit-sem"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "torch>=2.0.0",
    "transformers>=4.40.0",
    "sentence-transformers>=2.0.0"
]

[dependency-groups]
dev = [
    "pytest>=8.4.2",
]
